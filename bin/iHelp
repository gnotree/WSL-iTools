#!/usr/bin/env bash
cat <<'EOF'
=== Debian 12 iDevice Environment Loaded ===
Custom iDevice Commands Available:
  iAttach [BusId]                  Attach iPhone to WSL (usbipd)
  iHelp                            This help text
  iDeviceInfo                      Show device info (e.g., iDeviceInfo -k DeviceName)
  iDeviceSyslog                    Relay syslog (pipeable) (e.g., iDeviceSyslog | grep error)
  iDevicePair                      Manage pairings (e.g., iDevicePair pair)
  iDeviceBackup2                   Modern backups (e.g., iDeviceBackup2 backup --no-encrypt ~/ios_backup)

  iLive [-v] [-d 10m] [--zip]     Live-capture syslog to ~/Forestry/<ts>/syslog_<ts>.log
                                  -v shows logs; Ctrl-C to stop; -d/--duration auto-stops.
  iCopy [-v] [--zip]              Copy ALL .ips + diagnostics to ~/GNO-DATA/<ts>

  s                                Reload this .bashrc
  ss                               Elevate to root bash

Examples:
  iLive -v --duration 5m --zip
  iCopy --zip
EOF
